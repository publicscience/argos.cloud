[uwsgi]
socket = /tmp/{{ app_name }}.sock
logto = /var/log/{{ app_name }}.log
plugins = python3
virtualenv =  {{ venv_path }}
pythonpath = {{ app_path }}
wsgi-file = {{ app_path }}/application.py
callable = app
max-requests = 1000
chmod-socket = 666
chown-socket = www-data:www-data
processes = 2
master
